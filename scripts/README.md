# Scripts

This directory will hold various utility and maintenance scripts for the project.

Examples:
- `deploy.sh`: A script for deploying the GitHub App backend.
- `db_migrate.sh`: Scripts for running database migrations.
- `seed.ts`: A script to seed the database with initial data for development and testing.
