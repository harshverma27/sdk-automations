# Reusable Actions

This directory contains composite GitHub Actions that can be used directly in repository workflows. The primary action for this framework is `configure-automation`.

This allows repositories to trigger automation logic on a schedule or from other workflows, in addition to the event-driven hooks handled by the app backend.
