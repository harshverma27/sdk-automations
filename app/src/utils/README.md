# Utilities

This directory will hold low-level, reusable utility functions that can be used across the application.

Examples:
- `logger.ts`: The application-wide logger configuration.
- `errors.ts`: Custom error classes for better error handling.
- `dateUtils.ts`: Helper functions for date and time manipulation (e.g., calculating if an issue is stale).
